apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-api
  namespace: rubrica
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-api
  template:
    metadata:
      labels:
        app: auth-api
    spec:
      enableServiceLinks: false
      containers:
        - name: auth-api
          image: rubrica-auth-api:local
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: rubrica-config
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: auth-api
  namespace: rubrica
spec:
  selector:
    app: auth-api
  ports:
    - port: 8000
      targetPort: 8000
