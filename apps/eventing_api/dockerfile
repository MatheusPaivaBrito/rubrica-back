FROM python:3.14-slim

WORKDIR /app
COPY pyproject.toml poetry.lock README.md ./
RUN pip install --no-cache-dir poetry \
    && poetry config virtualenvs.create false \
    && poetry install --no-interaction --no-root
COPY . .
ENV PYTHONUNBUFFERED=1
ENV PYTHONPATH=apps/eventing_api/src:packages/shared_kernel/src
CMD ["sh", "-c", "exec gunicorn eventing_api.main:app -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --workers ${EVENTING_WEB_CONCURRENCY:-1}"]
